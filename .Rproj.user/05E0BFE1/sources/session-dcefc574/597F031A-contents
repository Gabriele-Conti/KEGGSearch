usethis::create_package("C:/Users/gabri/Documents/Dottorato_Lavoro/R_Script/KEGGPackage")
KEGGSearch <- function(x)

  
# Define required packages
required_packages <- c("devtools", "roxygen2", "testthat", "knitr")

# Install missing packages along with dependencies
for (pkg in required_packages) {
  if (!requireNamespace(pkg, quietly = TRUE)) {
    install.packages(pkg, dependencies = TRUE)
  }
}

library(usethis)
use_git_config(user.name = "Gabriele-Conti", user.email = "gabrieleconti.gc@outlook.it")
usethis::use_git()
usethis::use_readme_rmd()
usethis::use_mit_license()
